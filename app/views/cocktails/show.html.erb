<h1> Title: <%= @cocktail.name %></h1>

<%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>

<h2>These are the doses:</h2>
<ul class="list-group">
  <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item"><%= dose.description %> <%= dose.ingredient.name %>
      <%= link_to "Delete", dose_path(dose), method: :delete %>
    </li>
  <% end %>
</ul>
<h3>Add a dose damn it: <%= link_to "New Dose", new_cocktail_dose_path(@cocktail) %> </h3>
<h3> <%= link_to "Home", cocktails_path %> </h3>


